import uuid

# Generate a random UUID (version 4)
my_uuid = uuid.uuid4()
print(f"UUID object: {my_uuid}")

# Convert the UUID object to a string
my_uuid_string = str(uuid.uuid4())
print(f"UUID string: {my_uuid_string}")
